package services {
    package database {
        class PostgreSQLDatabase {
            - Client client
            - PostgreSQLDatabase postgreSQLDatabase
            - boolean _isConnected
            + void connect()
            + void close()
            + any query()
            + string getUserRole(string userId)
            + self getInstance()
        }
    }

    package validation {
        class TypeValidator {
            + bool isString(testedVariable: any, variableName: string)
        }
    }

    package errors {
        class BadRequestError {
            + string getMessage()
        }

        class InvalidRetrievedValuesError {
            + string getMessage()
        }

        class UnauthorisedActionError {
            + string getMessage()
        }
    }
}